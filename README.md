# NOTES
This is my first software in Node
```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     It's the base of the multiplying table      [number] [required]
  -l, --list     Show the table in console                             [boolean]
  -u, --until    Number until the base will be multiplied               [number]
```
